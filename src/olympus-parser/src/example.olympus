enum Action {
    DELETE->123;
    SECURE_DELETE->456;
    ENCRYPT->789;
}

struct File {
    path->@string;
}

rpc {
    proc getFiles() -> @string;
    proc meow(action->Action, version->@int8) -> @int8;
}
