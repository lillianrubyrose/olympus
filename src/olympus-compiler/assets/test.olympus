enum Action {
    DELETE->1;
    SECURE_DELETE->2;
    ENCRYPT->3;
}

struct File {
    path->@string;
    content->@array[@uint8];
}

rpc {
    proc getFiles(directory->@string, acterAction->Action) -> @array[File];
}
