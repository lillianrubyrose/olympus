enum Action {
    Delete->1;
    SecureDelete->2;
    Encrypt->3;
}

struct File {
    path->@string;
    extension->@option[@string];
    file_size->@varuint64;
    content->@array[@uint8];
}

rpc {
    proc getFiles(directory->@string, acterAction->Action) -> @array[File];
}
