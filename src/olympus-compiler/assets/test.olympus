enum Action {
    DELETE->123;
    SECURE_DELETE->456;
    ENCRYPT->789;
}

struct File {
    path->@string;
    something->FileSomething;
}

enum FileSomething {
    A->1;
    B->9;
}

rpc {
    proc getFiles() -> @array[@array[@array[File]]];
}
